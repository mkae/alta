Import('env')

sources = ['function.cpp']
env.SharedLibrary('#build/plugins/nonlinear_fresnel_schlick', sources,
                  LIBS = env['PLUGIN_LIB'])
Return('sources')               # always built
