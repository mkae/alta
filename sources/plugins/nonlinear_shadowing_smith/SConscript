Import('env')

sources = ['function.cpp']
env.SharedLibrary('#build/plugins/nonlinear_shadowing_smith', sources,
                  LIBS = env['PLUGIN_LIB'])
Return('sources')               # always built
